# Empty = same-origin fetches; Next.js rewrites in next.config.mjs proxy /api/* to BACKEND_URL.
# Required when the browser runs on a different host than the backend (VS Code Remote, network IP).
NEXT_PUBLIC_API_URL=
